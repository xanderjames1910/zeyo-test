import { Routes } from '@angular/router';
import { ProcessDashboardComponent } from './components/process-dashboard/process-dashboard.component';

export const routes: Routes = [
  { path: '', component: ProcessDashboardComponent },
];
